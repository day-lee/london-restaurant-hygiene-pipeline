# XML 파싱

## lxml 라이브러리 
- 트리 구조의 xml 파일을 읽어서 파싱함 
- 파싱된 XML은 부모-자식 관계를 가진 트리 구조로 접근 가능 
- XML 파일 전체는 트리, element는 나뭇잎, 각각의 태그(노드)를 의미함 

- root.xpath(".//EstablishmentDetail")를 사용하여 모든 EstablishmentDetail 노드를 리스트로 가져온다.
- 추출할 XML 태그 목록을 target_tags 리스트에 정의해 반복적으로 사용한다.
- 각 레코드마다 tag를 사용해 node를 찾고`record.find(tag)`, `node.text.strip()`로 value를 추출해서 값으로 할당해준다. 
- 최종적으로 list[dict] 형태의 데이터가 만들어진다.

### 예외 처리
레코드 리스트에서 태그 리스트를 중첩 루프로 돌면서 예외 처리
1. 노드가 없거나 값이 없는 경우
2. date는 형변환 해주기 

- 마지막에 xml파일에 명시된 아이템 개수와 실제 파싱된 객체의 개수가 일치 여부 airflow 실행 로그에서 확인 가능