## extract 

### 메모리의 데이터를 파일로 옮겨 담는 과정이 필요하다. 

- request 라이브러리로 GET 요청을 하면 바이너리 바이트로 메모리에 머문다. 
- 휘발성 데이터를 파일 입력 코드를 통해 메모리에 저장하는 과정이 필요하다.

- HTTP GET 요청함. `requests.get()`
- `timeout=30`, 응답 대기를 30초로 설정해 무한 대기를 막음 
- retry는 airflow가 매니징해줨 
- Response 객체가 응답으로 왔음 
- 식별용 유저 에이전트 선언 추가하기. 제너릭한 크롬 브라우저 내용. IP 차단 당지를 위해 매크로 아니고 파이프 라인입니다하고 알려주는 것.
- 세션 하나 열어놓고 33개 구 반복하며 파일 저장함. `session.get()`
- 실무에서는 airflow의 HttpHook을 사용하면 세션을유지해서 connection pool을 하나 열어놓고 대기함. 

- `with open() as f:` 즉, `with`는 try-finally block 을 쓰지 않고도 exception 상황에도 제대로 파일이 닫힐 수 있게 보장한다. 
- 파일 객체를 만들어준다. tmp로 경로 지정하고, ~~write mode로, utf-8 인코딩으로. utf-8은 바이너리를 사람의 언어로 안전하게 변환(인코딩/디코딩)해준다.~~ 
- encoding='utf-8' 삭제. extract 단계에서 굳이 인코딩해서 파일에 쓸 필요가 없다. 네트워크에서 들어올 떄 바이너리로 받아서 파일에 저장하고, 보낼때도 바이너리로 보내고, 가공 단계에서 사람 언어로 디코딩해주면 된다.  
- mode를 wb (write binary)로 지정하면 중간 번역과정이 생략되어 디스크에 기록 속도가 빠르다.

- response 객체를 `r.text`로 사람 언어인 텍스트로 디코딩해놓음. 사람이 바로 읽을 수 있음 utf-8 인코딩 필요. 만약 잘못된 문자가 섞이면 에러 날 수 있음. 
- `r.content`는 바이너리 원본임.
- 하드 디스크에는 바이너리로 저장되어 있지만, 텍스트 에디터가 자동 통역을 해서 사람 언어로 보임. 
- `xxd ./data/fsa.xml | head -n 5` 명령어를 넣으니 숫자가 나옴. `00000000: 3c3f 786d 6c20 7665 7273 696f 6e3d 2231  <?xml version="1` 바이너리 검증 완료. 

- tmp 경로는 도커와 에어플로우가 연관되어있다. 
- 내 컴퓨터에 임시 폴더를 만들고, 도커 컴포즈를 수정해서 서로 바라보도록 마운트해준다. 
- 연결한 도커 내부의 경로로 지정한다. 로컬 테스트 시에는 경로 로컬로 변경해서 실행해야함.
- 실제 파일이 내 컴퓨터에 생성되는지 테스트하기 

- api 요청 에러 핸들링
- 4xx, 5xx 등 에러로 파일 다운로드가 정상적으로 안되었을 떄, 에어플로우에게 에러 객체를 raise해서 retry등 후속 조치를 할 수 있게끔 해야한다. 안그러면 html 문구 등이 저장되어 다음 테스트에서 에러가 난다. 
- `raise_for_status()`는 if 문 없이 달아줘도 내부에 만약 200이 아니면 에러를 던지라는 코드가 내장되어 있음. 

- `requests.exceptions.HTTPError`는 서버와 대화는 했지만 4,5xx 상태코드가 왔을 경우
- `requests.exceptions.RequestException`은 응답을 아예 받지 못함. 인터넷 끊겼거나, 서버꺼져서 반응이 없거나. 

- session을 열어서 connection pooling, 세션 재사용이 가능해짐. `HTTP Keep-Alive` 상태임. handshake 오버헤드가 감소함. 

- 반복문에서 33개 구 중 누락되는 경우를 어떻게 핸들링 할 것인가?
- 루프에서 에러가 났을 때 바로 Raise로 airflow에 에러 신호를 주지 않고, continue로 일단 모든 루프를 돌게함. 
- 실패 기록을 리스트에 담아둠. 
- 일단 정상적인 구는 다 돌고나서, 마지막에 이 실패 리스트가 empty인지 확인한다. 
- 만약 실패한게 하나라도 있으면 강제로 raise를 던져서 task가 최종적으로는 실패하게 만든다. 
- 다시 시도할 때 누락된 구만 다시 채워넣는 구조를 만든다. -> os.path.exists() 이용해서 파일이 이미 존재하는지 확인함 
- raise는 가장 최근에 발생한 에러를 그대로 다시 던지겠다는 뜻. 